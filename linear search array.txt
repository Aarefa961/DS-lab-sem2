//Type your content here
#include<stdio.h>
int main(){
	int n,i, key;
	int arr[20];
	scanf("%d",&n);

	for(i=0 ; i<n;i++)
		{scanf("%d",&arr[i]);}
		
		scanf("%d",&key);
for(i=0 ;i< n;i++){
	if(arr[i]==key){
		printf("found at position %d\n",i);
		return 0;
	}
}
	
		printf("%d not found\n",key);
		return 0;
	

}